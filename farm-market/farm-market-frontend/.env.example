# Copy this file to .env and adjust as needed.

# Base URL of your Django + DRF API
VITE_API_BASE_URL=http://127.0.0.1:8000/api

# Keep true while building the UI without a backend.
# Set to false once your Django endpoints exist and you're ready
# to wire them up for real.
VITE_USE_MOCKS=true
